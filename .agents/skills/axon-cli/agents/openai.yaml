interface:
  display_name: "Axon Cli"
  short_description: "Repo-specific patterns and workflows for axon-cli"
  default_prompt: "Use the axon-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true